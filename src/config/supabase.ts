/**
 * Supabase configuration.
 *
 * This file contains the Supabase URL and anon key hard-coded into the frontend source.
 * Replace the placeholder strings with your actual values.
 *
 * Security note:
 * - The anon key is public and intended for client-side usage.
 * - Do NOT place your service_role (secret) key here.
 *
 * Example:
 * export const SUPABASE_URL = 'https://xyzcompany.supabase.co';
 * export const SUPABASE_ANON_KEY = 'your-anon-key';
 */

export const SUPABASE_URL = 'REPLACE_WITH_YOUR_SUPABASE_URL';
export const SUPABASE_ANON_KEY = 'REPLACE_WITH_YOUR_SUPABASE_ANON_KEY';
